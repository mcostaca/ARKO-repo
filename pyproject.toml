[project]
name = "arkoapp"
version = "0.1.0"
description = "Teste tecnico para a empresa ARKO"
authors = [
    {name = "Matheus",email = "matheus.alves@uncisal.edu.br"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django (>=5.2.4,<6.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
